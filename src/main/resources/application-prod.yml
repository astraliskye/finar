spring:
  application:
    name: finar
  datasource:
    url: jdbc:postgresql://localhost/finar
  jpa:
    hibernate:
      ddl-auto: create
    generate-ddl: true
  data:
    redis:
      host: localhost
      port: 6379
server:
  servlet:
    session:
      timeout: 600s
  port: 8000